{/* <Tooltip
									title="Messages"
									aria-label="messages"
									open={tooltipCtrl.message || false}
									disableHoverListener
									onMouseEnter={() => setTooltipCtrl({ message: true })}
									onMouseLeave={() => setTooltipCtrl({ message: false })}
									TransitionComponent={Zoom}
									classes={{ tooltip: classes.tooltip }}
								>
									<Button onClick={handlePopper("message")} aria-label="show 4 new mails" color="inherit" className="me-2 bg-primary-light" classes={{ root: classes.root }}>
										<Badge variant="dot" color="primary" size="small" classes={{ badge: classes.badge }}>
											<i className="fas fa-envelope"></i>
										</Badge>
									</Button>
								</Tooltip> */}

								{/* message popper */}
								{/* <NavPopper id="message-pop" open={popperControl["message"]} anchorEl={popAnchor} onClickAway={() => handlePopperClose("message")}>
									<div>
										<PopMessage />
									</div>
								</NavPopper> */}

								{/* <Tooltip
									title="Notifications"
									aria-label="notifications"
									open={tooltipCtrl.notification || false}
									disableHoverListener
									onMouseEnter={() => setTooltipCtrl({ notification: true })}
									onMouseLeave={() => setTooltipCtrl({ notification: false })}
									TransitionComponent={Zoom}
									classes={{ tooltip: classes.tooltip }}
								>
									<Button onClick={handlePopper("notification")} aria-label="show 17 new notifications" color="inherit" className="me-2 bg-primary-light" classes={{ root: classes.root }}>
										<Badge variant="dot" color="primary" size="small" classes={{ badge: classes.badge }}>
											<i className="fas fa-bell"></i>
										</Badge>
									</Button>
								</Tooltip> */}

								{/* notification popper */}
								{/* <NavPopper id="notification-pop" open={popperControl["notification"]} anchorEl={popAnchor} onClickAway={() => handlePopperClose("notification")}>
									<div>
										<PopNotification />
									</div>
								</NavPopper> */}